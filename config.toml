# https://www.getzola.org/documentation/getting-started/configuration/

# The URL the site will be built for
base_url = "https://blog.momo.gg"

# The site title and description; used in feeds by default.
title       = "Thinking out loud"
description = "Blazingly fast, frequently hyperbolic and confidently incorrect opinions of knowing shit about fuck."

theme = "terminimal"

# The default language; used in feeds.
default_language = "en"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# When set to "true", the generated HTML files are minified.
minify_html = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
    # Whether to do syntax highlighting
    # Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
    highlight_code = true

# Put all your custom variables here
# They will be available in your templates as `config.extra.my_variable`
[extra]
    # One of: blue, green, orange, pink, red.
    # Defaults to blue.
    accent_color = "blue"

    # One of: blue, dark, green, orange, pink, red.
    # Enabling dark background will also modify primary font color
    # to be darker.
    # Defaults to accent color (or, if not accent color specified, to blue).
    background_color = "dark"

    # The logo text - defaults to "Terminimal theme"
    logo_text = "Thinking out loud"

    # The logo link - defaults to base_url.
    # logo_home_link = "https://blog.momo.gg"

    author = "Momo"
